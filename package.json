{
  "name": "odyssey-validation-framework",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone validation framework for odyssey pages",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "test:validators": "playwright test --config=playwright.selftest.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^26.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "exceljs": "^4.4.0",
    "xlsx": "^0.18.5"
  }
}
